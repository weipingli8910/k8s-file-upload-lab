{{- if .Values.namespace.create }}
{{- /* 
  Note: Namespace creation via Helm can conflict if namespace already exists.
  Consider creating namespace separately or using kubectl create namespace --dry-run=client -o yaml | kubectl apply -f -
*/}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "file-upload-service.namespace" . }}
  labels:
    name: {{ include "file-upload-service.namespace" . }}
    environment: lab
{{- end }}

