apiVersion: v1
kind: Service
metadata:
  name: file-upload-service
  namespace: file-upload
  labels:
    app: file-upload-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: file-upload-service

